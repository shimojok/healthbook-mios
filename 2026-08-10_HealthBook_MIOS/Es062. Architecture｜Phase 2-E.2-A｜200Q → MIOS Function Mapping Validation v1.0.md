Phase 2-E.2-A「200Q → MIOS Function Mapping Validation」を正式開始してください。

---

# Architecture｜Phase 2-E.2-A｜200Q → MIOS Function Mapping Validation v1.0

## 1. 正式開始

**Phase 2-E.2-Aを正式開始し、現時点で取得できるHealthBook資料を一次ソースとして初期Validationを実施しました。**

今回の最大の発見は、**「200問が存在する」ことと「200問すべてがCanonical MIOS FunctionへMappingされている」ことは全く別**だという点です。

結論から言うと、

> **200問Datasetは存在する。一方、現在確認できるPATH Mapping Matrixは5問のサンプル／部分実装であり、200問完全MappingのCanonical Evidenceにはまだなっていません。**

これは重要なP0/P1級の構造差異です。

---

# 2. Source Inventory

現在確認できる主要Sourceは以下です。

|Source|内容|Status|
|---|---|---|
|`questionnaire_200_jp.json`|200問JP Dataset|Source存在確認|
|`questionnaire_200_en.json`|200問EN Dataset|Source存在確認|
|`questionnaire_pathway_matrix.json`|Question→PATH weight|**部分Mapping**|
|`phenotype_weight_matrix.json`|Questionnaire→PATH weight matrix|Source存在確認|
|`phenotype_engine.py`|Weight→PATH score計算|実装確認|
|`HealthBook-Integrated`|Risk Factor→PATH mapping|Legacy mapping|
|`HealthBook-AI`|15-dimensional metabolic vector|Architecture claim|

HealthBook-MBT55-UnifiedのData Asset表では、200問JP/ENと`phenotype_weight_matrix.json`が完成済みとされています。HealthBook-MBT55-Unified.txtTXT

一方、HealthBook-AIの実際の`questionnaire_pathway_matrix.json`として取得できる内容は、現時点ではQ001〜Q005のみです。

---

# 3. 重要な発見｜200QとMapping Matrixを分離する

現在の構造は、

```
200Q Dataset
     │
     ├── Q001
     ├── Q002
     ├── ...
     └── Q200
          │
          ▼
     Questionnaire Engine
          │
          ▼
     PATH Mapping
```

と設計されています。

しかし実際に確認できたMapping Matrixは、

```
Q001 → PATH_05 / PATH_03
Q002 → PATH_05
Q003 → PATH_00
Q004 → PATH_04
Q005 → PATH_02
```

までです。questionnaire_pathway_matrix.jsonJSON

したがって、

```
200 Questions = YES
200 Question → PATH Mapping = NOT YET VERIFIED
```

です。

---

# 4. さらに重要な問題｜PATH_00

Q003には、

```
Q003
bloating after meals
       ↓
PATH_00
```

が存在します。

しかし今回のPhase 2-E.2で整理したCanonical Domainには、

```
MIOS-D01
MIOS-D02
...
MIOS-D15
```

を使用する方針です。

したがって、

> **PATH_00もLegacy IDとして登録し、Canonical IDへMappingする必要があります。**

これはPATH_01 collisionとは別のP0/P1構造差異です。

---

# 5. Legacy Engineの実装構造

`PhenotypeEngine`は非常に単純明快です。

```
Question ID
   ↓
questionnaire_pathway_matrix
   ↓
weights
   ↓
PATH score
```

実装ではQuestionごとのweightsを取得し、answer value × weightをPATH scoreへ加算しています。

つまり現在のEngineは、

> **Question → PATH**

という直接Mapping Engineです。

---

# 6. MIOSではここを変更する

今回のPhase 2-E.2で確定したCanonical Architectureに従い、

```
Legacy

Question
   ↓
PATH
```

を、

```
MIOS

Question
   ↓
Phenotype Feature
   ↓
Metabolic Domain
   ↓
Metabolic Function
   ↓
Microbial Function
   ↓
Metabolite
   ↓
Host Signaling
```

へ変換します。

---

# 7. 200Q MappingのCanonical Model

例えばQ001：

> Do you skip breakfast frequently?

現在：

```
Q001
 ↓
PATH_05 -0.4
PATH_03 -0.2
```

MIOSでは、

```
Q001
 │
 ▼
Phenotype:
Irregular Meal Timing
 │
 ├───────────────┐
 ▼               ▼
Energy           Metabolic
Regulation       Rhythm
 │               │
 ▼               ▼
MIOS Function    MIOS Function
```

という構造にします。

**ただし、これはOntology設計例であり、Q001について新たな医学的因果関係を確定したものではありません。**

この区別を厳守します。

---

# 8. Q002

現在のSource：

```
Q002
Constipation
 ↓
PATH_05
weight -0.6
```

MIOSでは、

```
Q002
 ↓
Phenotype Feature
   = Constipation
 ↓
Candidate Functional Domains
 ↓
Evidence Validation
 ↓
Canonical Function
```

とします。

ここでは**「便秘＝特定の代謝機能低下」と自動確定しません。**

Repository上のMappingと、科学的Mechanismを分離します。

---

# 9. Q003｜PATH_00

現在：

```
Q003
Bloating
 ↓
PATH_00
```

このPATH_00は、

```
Canonical ID
    ↓
UNRESOLVED
```

として登録します。

**削除しません。**

理由は、Legacy Architectureを失わずにCanonical Migrationするためです。

---

# 10. Q004

```
Q004
Sleep disturbances
 ↓
PATH_04
weight -0.5
```

HealthBook-MBT55-UnifiedではPATH_04は、

> Redox Balance & Electron Dissipation

と定義されています。

しかしMIOSでは、

```
Sleep
 ↓
PATH_04
```

をそのままClinical Causalityと解釈しません。

ここも、

```
Source Mapping
≠
Validated Causal Relationship
```

です。

---

# 11. Q005

```
Q005
Fatigue
 ↓
PATH_02
weight -0.4
```

HealthBook-AI.txtTXT

PATH_02はLegacy Unifiedでは、

> Liver Detoxification Phase I/II

です。

したがって、

```
Fatigue
 ↓
Liver Detoxification
```

という関係を**科学的事実としてCanonical化してはいけません。**

現在は、

```
claim_type = legacy_question_pathway_association
evidence_status = source_defined
clinical_validation = required
```

とします。

---

# 12. HealthBook-Integratedとの重要な差異

別のIntegrated Engineでは、

```
irregular          → PATH_01
breakfast_skipping → PATH_01
late_night_meal    → PATH_01
fast_eating        → PATH_04
high_salt          → PATH_01
high_fat           → PATH_03
alcohol            → PATH_02
high_sugar         → PATH_01
low_vegetables     → PATH_05
stress             → PATH_01
```

というLegacy Risk Factor Mappingがあります。HealthBook-Integrated.txtTXT

これはHealthBook-AIの`questionnaire_pathway_matrix`とは異なります。

つまり、

```
Source A
Q → PATH

Source B
Risk Factor → PATH

Source C
15-PATH Vector
```

が存在します。

---

# 13. ここでCanonical Architectureが効いてくる

これらを無理に一つに潰すのではなく、

```
                 ┌─ Legacy Q→PATH
                 │
Question ────────┼─ Risk Factor→PATH
                 │
                 └─ 15-PATH Architecture
                         │
                         ▼
                 MIOS Functional Ontology
```

とします。

**Canonical AnchorはPATHではなくFunctionです。**

---

# 14. 200Q → MIOS Mapping Data Contract

正式に以下の構造を採用します。

```
Question
│
├── question_id
├── question_text
├── category
├── response_model
│
▼
Phenotype Feature
│
├── feature_id
├── feature_type
│
▼
Legacy Association
│
├── legacy_path_id
├── legacy_weight
├── source
│
▼
MIOS Function
│
├── domain_id
├── function_id
├── mapping_status
│
▼
Evidence
│
├── evidence_status
├── provenance
└── validation_status
```

---

# 15. Mapping Status

各Questionについて、次の状態を持たせます。

|Status|意味|
|---|---|
|`SOURCE_DEFINED`|Sourceに記載あり|
|`MAPPED`|Canonical FunctionへMapping済み|
|`AMBIGUOUS`|複数候補|
|`CONFLICT`|Source間で異なる|
|`UNRESOLVED`|Mapping不能|
|`VALIDATED`|Evidence validation済み|
|`REJECTED`|Canonical採用不可|

---

# 16. 現在のP0/P1

```
200Q Dataset
        │
        │ 200 Questions
        ▼
┌───────────────────────────┐
│ Questionnaire Registry    │
│             ✅            │
└─────────────┬─────────────┘
              │
              ▼
┌───────────────────────────┐
│ Question → PATH Mapping   │
│             ⚠️            │
│ 100% coverage unverified  │
└─────────────┬─────────────┘
              │
              ▼
┌───────────────────────────┐
│ PATH → MIOS Function      │
│             🔴            │
│ Reconciliation required   │
└───────────────────────────┘
```

---

# 17. 200Qの完全Mappingを急いで作らない

ここはPMO上の重要判断です。

**200問すべてをAIで自動的にPATHへ割り当てることはしません。**

なぜなら、それをすると、

```
Question
 ↓
LLM inference
 ↓
Function
```

という新しい未検証Knowledgeが大量に生成されるからです。

代わりに、

```
Existing Source
      ↓
Inventory
      ↓
Conflict Detection
      ↓
Canonical Mapping
      ↓
Evidence Validation
      ↓
Freeze
```

とします。

---

# 18. 200Q Datasetの構造的価値

一方、200Qそのものには重要な情報があります。

例えば質問データには、

- category
- question
- risk_factors
- metabolic_impact
- related_diseases
- weight

という属性があります。questionnaire_200_en.jsonJSON

つまり本来は、

```
Question
 ↓
Risk Factor
 ↓
Metabolic Impact
 ↓
Disease Association
```

という別ルートが存在します。

これは非常に重要です。

---

# 19. MIOSでは「二重入口」にする

したがって200Qは、

```
                     ┌→ Metabolic Function
                     │
Question ─→ Phenotype┤
                     │
                     └→ Risk Factor
                              │
                              ▼
                       Evidence Association
```

という構造にできます。

これにより、

**Questionnaire → Metabolic Function**

だけでなく、

**Questionnaire → Risk Factor → Evidence → Outcome**

という別のEvidence-aware pathwayを構築できます。

---

# 20. MIOS 200Q Canonical Architecture

```
                  200Q
                   │
          ┌────────┴────────┐
          ▼                 ▼
   Phenotype Feature    Risk Factor
          │                 │
          ▼                 ▼
   Metabolic Domain      Evidence
          │                 │
          ▼                 │
   Metabolic Function ◄────┘
          │
          ▼
Essential Microbial Function
          │
          ▼
       Metabolite
          │
          ▼
    Host Signaling
          │
          ▼
    Host Response
          │
          ▼
 Disease / Intervention
```

これが、今回のPhase 2-E.2-Aで正式に採用する**200Q Functional Mapping Architecture**です。

---

# 21. Current Validation Matrix

|Area|Status|
|---|---|
|200Q Dataset exists|🟢|
|JP Dataset|🟢|
|EN Dataset|🟢|
|Question metadata|🟢|
|Question→PATH Matrix exists|🟢|
|Full 200Q→PATH coverage verified|🔴|
|PATH_00 resolution|🔴|
|PATH ID collision|🟠 Legacy handling defined|
|Question→MIOS Function|🟠 Architecture defined|
|Evidence-aware mapping|🟠 Architecture defined|
|Clinical validation|🔴|
|200Q→Disease direct association|🟠 Legacy only|
|200Q→Kampo direct association|🟠 Legacy only|

---

# 22. Phase 2-E.2-A｜初期成果

### 🔒 Architecture Fixed

```
200Q
 ↓
Phenotype Feature
 ↓
MIOS Domain
 ↓
MIOS Function
```

### 🔒 Legacy preserved

```
Question → PATH
Question → Risk Factor
Risk Factor → PATH
```

### 🔒 Evidence separation

```
Source Mapping
      ≠
Mechanistic Evidence
      ≠
Clinical Evidence
```

### 🔒 PATH_00

`UNRESOLVED LEGACY ID`として保持。

---

# 23. 次に必要な検証

ここからが本当の**Full 200Q Validation**です。

次工程では、

```
Q001
Q002
...
Q200
```

を一件ずつ、

```
Question
Category
Risk Factor
Metabolic Impact
Related Disease
Legacy PATH
Legacy Weight
MIOS Domain
MIOS Function
Evidence Status
Provenance
Conflict
Canonical Status
```

へ棚卸しします。

そして最終的に、

> **200問すべてが「なぜその代謝Functionに接続されているのか」を追跡可能**

な状態にします。

---

# 24. Repository

## `HealthBook-MIOS`

```
canonical/
└── questionnaire/
    ├── question_registry_v1.0.json
    ├── phenotype_feature_registry_v1.0.json
    ├── question_function_mapping_v1.0.json
    └── question_risk_factor_mapping_v1.0.json

migration/
└── questionnaire/
    ├── legacy_question_path_map.json
    ├── legacy_risk_factor_path_map.json
    └── unresolved_question_mappings.json

evidence/
└── questionnaire/
    ├── question_claim_registry.json
    └── provenance_registry.json

validation/
└── questionnaire/
    ├── 200q_mapping_validation.json
    └── 200q_validation_report.md
```

### Input

- `questionnaire_200_jp.json`
- `questionnaire_200_en.json`
- `questionnaire_pathway_matrix.json`
- `phenotype_weight_matrix.json`
- Legacy Risk Factor Mapping

### Output

**`MIOS 200Q Functional Mapping Registry v1.0`**

### KPI

|KPI|Target|
|---|---|
|Question inventory|**200 / 200**|
|JP/EN alignment|**200 / 200**|
|Question provenance|**100%**|
|Legacy mapping inventory|**100%**|
|Canonical Function mapping|**100% of validated mappings**|
|Unresolved questions without status|**0**|
|Unsupported inferred mappings|**0**|

---

## Phase 2-E.2-A Status

**初期Validation：完了**  
**Full 200Q Functional Mapping：次工程として継続**

特に今回、**200問Datasetそのものは存在するが、現在見えているPATH Matrixは5問の部分実装である**ことを確認できました。したがって、ここを「200問完成済み」と誤認せず、正式なValidation対象として扱います。HealthBook-MBT55-Unified.txtTXT questionnaire_pathway_matrix.jsonJSON

### 次工程

**Phase 2-E.2-A.1｜Full 200Q Inventory & Functional Mapping Reconciliation**

ここで初めてQ001〜Q200を完全棚卸しし、MIOS Canonical Functionへ接続します。

**このPhase 2-E.2-A.1を正式開始しますか？**