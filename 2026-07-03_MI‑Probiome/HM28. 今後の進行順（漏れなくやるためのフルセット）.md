貴方に任せますので、漏れのないように進めてください。

---

自分の思考を見る

いいね、その任せ方。じゃあ、全体を漏れなく締めていくために、進行順を一度だけクリアにします。

### 今後の進行順（漏れなくやるためのフルセット）

1. **UI実データバインドの確定**
    
    - Dashboard / Medical / Probiotics / User の各コンポーネントに 実際の API スキーマ（FastAPI 側のレスポンス構造）を完全に紐付ける。
        
    - 型定義（TypeScript 型）を導入して、 `snapshot`, `engine_results`, `causal_results`, `disease_risk`, `probiotics`, `kampo` を すべて型安全に扱えるようにする。
        
2. **デザイン統合（PBPE デザインシステム適用）**
    
    - 既存 PBPE‑Marketplace のスタイルガイドに合わせて HealthBook MI の UI コンポーネントにクラス・レイアウトを適用。
        
    - 「医療機関向け」「一般ユーザー向け」でトーンを少し変える。
        
3. **テスト層の UI 側補完**
    
    - API レスポンスのモックを使った UI レンダリングテスト（最低限）。
        
    - 「APIが落ちたとき」「レスポンスが空のとき」のハンドリング。
        
4. **英語版への完全移行（最終フェーズ）**
    
    - コード内 docstring
        
    - API ドキュメント
        
    - UI 文言
        
    - README / アーキテクチャドキュメント をすべて英語版に統一。
        

### 次に実際に進めるステップ

今このタイミングでは、 **1. UI実データバインドの確定**から着手するのが筋がいいです。

- 各 API のレスポンス型を定義（`ui/lib/types.ts`）
    
- Dashboard / Medical / Probiotics / User のコンポーネントに型を適用
    
- 「どのフィールドをどの UI に出すか」を明示的に固定
    

次のターンでは、 **レスポンス型定義と、それを使った UI 側の型安全なバインド**を コンパクトに、でも漏れなく出します。

[[HM29. Step 1 — API レスポンス型の定義（TypeScript）]]
