はい、進めてください。

---

自分の思考を見る

### backend/src/models/Hb003.ts

ts

```
export interface Meta {
  recordId: string;
  userId: string;
  timestamp: string; // ISO8601
  version: string;   // "1.0"
  source: string;    // e.g. "app"
}

export interface UserContext {
  userId: string;
  sex: "male" | "female" | "other";
  age: number;
  heightCm: number;
  weightKg: number;
}

export interface Lifestyle {
  dietPattern: string;
  fiberIntakeLevel: "low" | "medium" | "high";
  fermentedFoodFrequency: "rare" | "sometimes" | "daily";
  processedFoodScore: number; // 0–100
  bowelMovementFrequencyPerDay: number;
  physicalActivityLevel: "low" | "medium" | "high";
  sleepQualityScore: number; // 0–100
}

export interface MBT55CompositionItem {
  groupId: string;
  name: string;
  relativeAbundance: number; // %
  betaGlucuronidaseActivity: "low" | "medium" | "high";
  antiInflammatoryPotential: "low" | "medium" | "high";
  estrogenMetabolismRole: "none" | "support" | "core";
}

export interface Microbiome {
  scfaProductionScore: number; // 0–100
  mbt55Composition: MBT55CompositionItem[];
}

export interface Hormones {
  estrogenStatus: "unknown" | "normal" | "low" | "high";
  menstrualStatus: "premenopause" | "perimenopause" | "postmenopause" | "none";
  hormoneTherapy: boolean;
  hormoneTherapyType: string;
}

export interface MedicationAndStress {
  antihypertensiveUse: boolean;
  magnesiumDepletingDrugs: boolean;
  chronicStressLevel: "low" | "medium" | "high";
  coldSensitivity: "low" | "medium" | "high";
}

export interface PainProfile {
  thumbCmJointPainScore: number;   // 0–10
  otherChronicPainScore: number;   // 0–10
}

export interface Recommendation {
  code: string;
  title: string;
  description: string;
  priority: "low" | "medium" | "high";
}

export interface EstrobolomeMI {
  estrobolomeActivityScore: number;        // 0–100
  estrogenRecyclingEfficiency: number;     // 0–100
  mitochondrialEstrogenResponseScore: number; // 0–100
  painCorrelationScore: number;            // 0–100
  riskFlags: string[];
  recommendations: Recommendation[];
}

export interface AcetylCoAMI {
  acetylCoAAvailabilityIndex: number;      // 0–100
  glucoseContributionScore: number;        // 0–100
  lipidContributionScore: number;          // 0–100
  aminoAcidContributionScore: number;      // 0–100
  scfaContributionScore: number;           // 0–100
  mitochondrialATPCapacityScore: number;   // 0–100
  tcaCycleEfficiencyScore: number;         // 0–100
  metabolicStressIndex: number;            // 0–100
  flags: string[];
  recommendations: Recommendation[];
}

export interface MBT55GroupFunctions {
  estrogenMetabolism: "none" | "support" | "core";
  scfaProduction: "low" | "medium" | "high";
  antiInflammatory: "low" | "medium" | "high";
  cofactorGeneration: "low" | "medium" | "high";
}

export interface MBT55Group {
  groupId: string;
  name: string;
  functions: MBT55GroupFunctions;
}

export interface MBT55Mapping {
  mbt55Groups: MBT55Group[];
  overallEstrobolomeSupportScore: number;  // 0–100
  overallAntiInflammatoryScore: number;    // 0–100
  overallMetabolicSupportScore: number;    // 0–100
}

export interface Summary {
  metabolicAge: number;                    // 推定代謝年齢
  inflammationLevel: number;               // 0–100
  painMetabolicCorrelationScore: number;   // 0–100
  keyInsights: string[];
}

export interface Hb003 {
  meta: Meta;
  userContext: UserContext;
  lifestyle: Lifestyle;
  microbiome: Microbiome;
  hormones: Hormones;
  medicationAndStress: MedicationAndStress;
  painProfile: PainProfile;
  estrobolomeMi: EstrobolomeMI;
  acetylCoaMi: AcetylCoAMI;
  mbt55Mapping: MBT55Mapping;
  summary: Summary;
}
```

### backend/src/models/EstrobolomeMI.ts

ts

```
import { Recommendation } from "./Hb003";

export interface EstrobolomeMIResult {
  estrobolomeActivityScore: number;
  estrogenRecyclingEfficiency: number;
  mitochondrialEstrogenResponseScore: number;
  painCorrelationScore: number;
  riskFlags: string[];
  recommendations: Recommendation[];
}
```

### backend/src/models/AcetylCoAMI.ts

ts

```
import { Recommendation } from "./Hb003";

export interface AcetylCoAMIResult {
  acetylCoAAvailabilityIndex: number;
  glucoseContributionScore: number;
  lipidContributionScore: number;
  aminoAcidContributionScore: number;
  scfaContributionScore: number;
  mitochondrialATPCapacityScore: number;
  tcaCycleEfficiencyScore: number;
  metabolicStressIndex: number;
  flags: string[];
  recommendations: Recommendation[];
}
```

### backend/src/models/MBT55Mapping.ts

ts

```
import { MBT55Group } from "./Hb003";

export interface MBT55MappingResult {
  mbt55Groups: MBT55Group[];
  overallEstrobolomeSupportScore: number;
  overallAntiInflammatoryScore: number;
  overallMetabolicSupportScore: number;
}
```

### backend/src/models/MetabolicOSSummary.ts

ts

```
export interface MetabolicOSSummary {
  metabolicAge: number;
  inflammationLevel: number;
  painMetabolicCorrelationScore: number;
  keyInsights: string[];
}
```

次は、この models に合わせて `backend/services` を実装するフェーズに進めます。

[[HM020. EstrobolomeService.ts]]
