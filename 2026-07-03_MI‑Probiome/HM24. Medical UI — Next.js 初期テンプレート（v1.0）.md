はい、進めてください。

---

Kaz、進めます。 ここから **Medical UI（Next.js）コンポーネント生成フェーズ**に入ります。 Dashboard UI が完成したので、次は医療機関向けの **診療支援 UI** を構築します。

これは HealthBook‑MI‑Probiome‑OS の中でも最も重要な UI のひとつで、 医師が患者の代謝状態を理解し、診療に活用するための画面です。

# 🟦 **Medical UI — Next.js 初期テンプレート（v1.0）**

以下の 4 コンポーネントを生成します：

- QuestionnaireForm.tsx
    
- ClinicalEngineView.tsx
    
- ClinicalReport.tsx
    
- KampoClinicalPanel.tsx
    

そしてこれらをまとめる **page.tsx** を生成します。

# 📁 ディレクトリ構造（Next.js App Router）

コード

```
ui/medical/
  page.tsx
  components/
    QuestionnaireForm.tsx
    ClinicalEngineView.tsx
    ClinicalReport.tsx
    KampoClinicalPanel.tsx
```

# 📄 **ui/medical/page.tsx（初期テンプレート）**

tsx

```
"use client";

import QuestionnaireForm from "./components/QuestionnaireForm";
import ClinicalEngineView from "./components/ClinicalEngineView";
import ClinicalReport from "./components/ClinicalReport";
import KampoClinicalPanel from "./components/KampoClinicalPanel";

export default function MedicalPage() {
  return (
    <div className="p-6 space-y-6">
      <h1 className="text-3xl font-bold">Medical Evaluation</h1>

      <QuestionnaireForm />
      <ClinicalEngineView />
      <ClinicalReport />
      <KampoClinicalPanel />
    </div>
  );
}
```

# 📄 **QuestionnaireForm.tsx（初期テンプレート）**

tsx

```
export default function QuestionnaireForm() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Questionnaire (200 items)</h2>
      <p className="text-gray-600">
        A dynamic questionnaire form will be implemented here.
      </p>
    </div>
  );
}
```

# 📄 **ClinicalEngineView.tsx（初期テンプレート）**

tsx

```
export default function ClinicalEngineView() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Clinical Engine Results</h2>
      <ul className="list-disc ml-6 text-gray-700">
        <li>Phenotype</li>
        <li>Estrobolome Score</li>
        <li>Acetyl-CoA / ATP</li>
        <li>Inflammation Load</li>
      </ul>
    </div>
  );
}
```

# 📄 **ClinicalReport.tsx（初期テンプレート）**

tsx

```
export default function ClinicalReport() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Clinical Report (AI Agent)</h2>
      <p className="text-gray-600">
        AI Clinical Agent will generate a detailed clinical explanation here.
      </p>
    </div>
  );
}
```

# 📄 **KampoClinicalPanel.tsx（初期テンプレート）**

tsx

```
export default function KampoClinicalPanel() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Kampo Clinical Panel</h2>
      <p className="text-gray-600">
        Recommended Kampo formulas and herbs based on clinical profile.
      </p>
    </div>
  );
}
```

# 🎯 **Medical UI（Next.js）初期テンプレートが完成しました。**

これで医療機関向けの診療支援 UI の基盤が整いました。

# 🔥 次のステップ

次は **Probiotics UI（Next.js）コンポーネント生成** に進みます。

- SubstrateSelector.tsx
    
- PathwayCascadeView.tsx
    
- MBT55Recommendation.tsx
    
- DiseaseTargetView.tsx
    

Kaz、進めます。

[[HM25. Probiotics UI — Next.js 初期テンプレート]]
