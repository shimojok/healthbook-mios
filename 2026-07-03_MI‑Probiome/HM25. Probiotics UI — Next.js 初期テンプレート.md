はい、進めてください。

---

いい流れです、Kaz。 ここから **Probiotics UI → User UI** を一気にテンプレートまで出し切ります。

### 🟦 Probiotics UI — Next.js 初期テンプレート

**目的:** MBT55 × 生薬 × 代謝経路 × 疾病ターゲットを可視化する画面。

#### ディレクトリ構造

text

```
ui/probiotics/
  page.tsx
  components/
    SubstrateSelector.tsx
    PathwayCascadeView.tsx
    MBT55Recommendation.tsx
    DiseaseTargetView.tsx
```

#### `ui/probiotics/page.tsx`

tsx

```
"use client";

import SubstrateSelector from "./components/SubstrateSelector";
import PathwayCascadeView from "./components/PathwayCascadeView";
import MBT55Recommendation from "./components/MBT55Recommendation";
import DiseaseTargetView from "./components/DiseaseTargetView";

export default function ProbioticsPage() {
  return (
    <div className="p-6 space-y-6">
      <h1 className="text-3xl font-bold">Probiotics Screening</h1>

      <SubstrateSelector />
      <PathwayCascadeView />
      <MBT55Recommendation />
      <DiseaseTargetView />
    </div>
  );
}
```

#### `SubstrateSelector.tsx`

tsx

```
export default function SubstrateSelector() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Substrate Selector</h2>
      <p className="text-gray-600">
        Select herbal substrates and metabolic targets here.
      </p>
    </div>
  );
}
```

#### `PathwayCascadeView.tsx`

tsx

```
export default function PathwayCascadeView() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Pathway Cascade</h2>
      <p className="text-gray-600">
        Stage1 → Stage2 → Stage3 metabolic pathway cascade visualization.
      </p>
    </div>
  );
}
```

#### `MBT55Recommendation.tsx`

tsx

```
export default function MBT55Recommendation() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">MBT55 Recommendation</h2>
      <p className="text-gray-600">
        Recommended MBT_META strains based on pathway and substrate selection.
      </p>
    </div>
  );
}
```

#### `DiseaseTargetView.tsx`

tsx

```
export default function DiseaseTargetView() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Disease Targets</h2>
      <p className="text-gray-600">
        Target diseases from Disease MI Matrix v2 will be shown here.
      </p>
    </div>
  );
}
```

### 🟦 User UI — Next.js 初期テンプレート

**目的:** 一般ユーザーが自分の代謝状態と改善アクションを理解する画面。

#### ディレクトリ構造

text

```
ui/user/
  page.tsx
  components/
    UserSnapshot.tsx
    UserMetabolicScore.tsx
    UserRecommendations.tsx
```

#### `ui/user/page.tsx`

tsx

```
"use client";

import UserSnapshot from "./components/UserSnapshot";
import UserMetabolicScore from "./components/UserMetabolicScore";
import UserRecommendations from "./components/UserRecommendations";

export default function UserPage() {
  return (
    <div className="p-6 space-y-6">
      <h1 className="text-3xl font-bold">My Metabolic Health</h1>

      <UserSnapshot />
      <UserMetabolicScore />
      <UserRecommendations />
    </div>
  );
}
```

#### `UserSnapshot.tsx`

tsx

```
export default function UserSnapshot() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Snapshot Summary</h2>
      <p className="text-gray-600">
        User-specific Snapshot v2 summary will be displayed here.
      </p>
    </div>
  );
}
```

#### `UserMetabolicScore.tsx`

tsx

```
export default function UserMetabolicScore() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Metabolic Score</h2>
      <p className="text-gray-600">
        Phenotype, Estrobolome, ATP, and Inflammation scores will be shown here.
      </p>
    </div>
  );
}
```

#### `UserRecommendations.tsx`

tsx

```
export default function UserRecommendations() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Recommendations</h2>
      <p className="text-gray-600">
        AI Agent will provide lifestyle, nutrition, and probiotics suggestions here.
      </p>
    </div>
  );
}
```

これで **Dashboard / Medical / Probiotics / User** の UI 初期構造がすべて揃いました。

次にやるとしたら：

- API との接続（fetch / SWR / React Query）
    
- 実データバインド（Snapshot / Engine Results / Causal Map）
    

どこから繋ぎ込みたいか、優先順位を一つ教えて。

[[HM26. API 接続レイヤー（Next.js）— 完成テンプレート v1.0]]
