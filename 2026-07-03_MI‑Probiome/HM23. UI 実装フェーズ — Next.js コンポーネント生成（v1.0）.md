進めてください。

---

Kaz、進めます。 ここから **UI 実装フェーズ（Next.js コンポーネント生成）**に正式に入ります。 フェーズ2の UI ワイヤーフレームはすでに完成しているので、 次は **実際に使える Next.js コンポーネントの初期テンプレート（v1.0）** を生成します。

あなたの開発スタイル（PMOレベルの正確性・完全ファイル生成・部分編集を嫌う）に合わせて、 **各 UI の「完全な初期ファイル構造＋コンポーネントテンプレート」** を提供します。

# 🟦 **UI 実装フェーズ — Next.js コンポーネント生成（v1.0）**

以下の 4 つの UI を順番に実装します：

1. **Dashboard UI**
    
2. **Medical UI**
    
3. **Probiotics UI**
    
4. **User UI**
    

まずは **Dashboard UI** から開始します。

# 🟦 **1. Dashboard UI — Next.js 初期テンプレート（v1.0）**

あなたの PBPE‑Marketplace と統合するための **代謝OSの中心画面**です。

## 📁 ディレクトリ構造（Next.js App Router）

コード

```
ui/dashboard/
  page.tsx
  components/
    SnapshotCard.tsx
    EngineSummary.tsx
    CausalMapView.tsx
    DiseaseRiskMap.tsx
    ProbioticsPanel.tsx
    KampoPanel.tsx
```

# 📄 **ui/dashboard/page.tsx（初期テンプレート）**

tsx

```
"use client";

import SnapshotCard from "./components/SnapshotCard";
import EngineSummary from "./components/EngineSummary";
import CausalMapView from "./components/CausalMapView";
import DiseaseRiskMap from "./components/DiseaseRiskMap";
import ProbioticsPanel from "./components/ProbioticsPanel";
import KampoPanel from "./components/KampoPanel";

export default function DashboardPage() {
  return (
    <div className="p-6 space-y-6">
      <h1 className="text-3xl font-bold">HealthBook MI Dashboard</h1>

      <SnapshotCard />
      <EngineSummary />
      <CausalMapView />
      <DiseaseRiskMap />
      <ProbioticsPanel />
      <KampoPanel />
    </div>
  );
}
```

# 📄 **SnapshotCard.tsx（初期テンプレート）**

tsx

```
export default function SnapshotCard() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Metabolic Snapshot</h2>
      <p className="text-gray-600">
        Snapshot v2 summary will be displayed here.
      </p>
    </div>
  );
}
```

# 📄 **EngineSummary.tsx（初期テンプレート）**

tsx

```
export default function EngineSummary() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Engine Summary</h2>
      <ul className="list-disc ml-6 text-gray-700">
        <li>Phenotype</li>
        <li>Estrobolome Score</li>
        <li>Acetyl-CoA / ATP</li>
        <li>Inflammation Load</li>
      </ul>
    </div>
  );
}
```

# 📄 **CausalMapView.tsx（初期テンプレート）**

tsx

```
export default function CausalMapView() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Causal Map</h2>
      <p className="text-gray-600">
        MBT55 → Estrobolome → AcetylCoA → ATP → Inflammation → Disease Risk
      </p>
    </div>
  );
}
```

# 📄 **DiseaseRiskMap.tsx（初期テンプレート）**

tsx

```
export default function DiseaseRiskMap() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Disease Risk Map</h2>
      <p className="text-gray-600">
        137-disease risk heatmap will be displayed here.
      </p>
    </div>
  );
}
```

# 📄 **ProbioticsPanel.tsx（初期テンプレート）**

tsx

```
export default function ProbioticsPanel() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Probiotics Recommendation</h2>
      <p className="text-gray-600">
        MBT55 × Herbal substrate × Pathway cascade results.
      </p>
    </div>
  );
}
```

# 📄 **KampoPanel.tsx（初期テンプレート）**

tsx

```
export default function KampoPanel() {
  return (
    <div className="border rounded-lg p-4 shadow-sm">
      <h2 className="text-xl font-semibold mb-2">Kampo Recommendation</h2>
      <p className="text-gray-600">
        Recommended formulas and herbs based on metabolic profile.
      </p>
    </div>
  );
}
```

# 🎯 **Dashboard UI（Next.js）初期テンプレートが完成しました。**

これで PBPE‑Marketplace に **HealthBook MI Dashboard** を追加する準備が整いました。

# 🔥 次のステップ

次は **Medical UI（Next.js）コンポーネント生成** に進みます。

- QuestionnaireForm.tsx
    
- ClinicalEngineView.tsx
    
- ClinicalReport.tsx
    
- KampoClinicalPanel.tsx
    

Kaz、進めます。

[[HM24. Medical UI — Next.js 初期テンプレート（v1.0）]]
