schema.jsonコードはコンパクトなのですね。 ギャップ確認からお願いします。

---

Kaz、了解。  
ここからは **“ギャップ確認（Gap Analysis）”** を、あなたの既存 JSON（GitHub）と、私が作成した **3つの schema v1.0** の間で **体系的に行います**。

これは HealthBook‑MI を正しく構築するための **最重要ステップ** です。

---

# 🧩 **ギャップ分析（Gap Analysis）— 3つのスキーマ × 既存JSON**

---

# 1️⃣ **questionnaire_200.schema.json と既存 JSON のギャップ**

### ✔ GitHub の実データ

- `questionnaire_200_jp.new.json`
- `questionnaire_200_en.new.json`

### ✔ 現状の構造

- 質問ID
- カテゴリ
- サブカテゴリ
- 質問文
- 回答形式（Yes/No, Scale など）
- 重み（weight）
- 英語版・日本語版の2種類

### ✔ スキーマとの一致点

- `q_id`
- `category`
- `sub_category`
- `text`
- `type`
- `options`
- `weight`

→ **ほぼ完全一致。スキーマ化に問題なし。**

### ❗ ギャップ（不足しているもの）

**1. version 情報がない**  
→ スキーマでは `version` を必須にしている。

**2. questionnaire_id がない**  
→ スキーマでは `questionnaire_id` を必須にしている。

**3. 言語情報（jp/en）が JSON 内に明示されていない**  
→ スキーマでは `language` を必須にしている。

### ✔ 対応方針

- `questionnaire_id`: `"HB-Q200"` を付与
- `version`: `"1.0"` を付与
- `language`: `"jp"` / `"en"` を付与

→ **自動付与で問題なし。構造は完璧。**

---

# 2️⃣ **disease_risk_137.schema.json と既存 JSON のギャップ**

### ✔ GitHub の実データ

- `disease_matrix_137.json.md`（Markdown形式）

### ✔ 現状の構造

Markdown 内に：

- 疾病名
- リスク因子
- 関連する問診項目
- スコア
- カテゴリ

が **表形式で記載**されている。

### ✔ スキーマとの一致点

- `disease_id`
- `name_jp`
- `risk_factors.questionnaire`
- `risk_factors.blood`
- `risk_factors.phenotyping`

→ **概念は完全一致。**

### ❗ ギャップ（不足しているもの）

**1. JSON ではなく Markdown である**  
→ スキーマは JSON を要求する。

**2. threshold（low/medium/high）が存在しない**  
→ スキーマでは必須。

**3. disease_id が付与されていない**  
→ スキーマでは必須。

**4. 英語名（name_en）がない**  
→ スキーマでは任意だが、将来の国際化で必要。

### ✔ 対応方針

- Markdown → JSON 変換が必要
- disease_id を `"D001"〜"D137"` で付与
- threshold は後で設定（あなたの医学的知見が必要）
- name_en は後で追加可能

→ **構造化すればスキーマに完全適合する。**

---

# 3️⃣ **kampo_mbt55.schema.json と既存 JSON のギャップ**

### ✔ GitHub の実データ

- `kampo_metabolic_library.json`

### ✔ 現状の構造

この JSON は非常に優秀で：

- 生薬（herbs）
- 方剤（formulae）
- MBT55
- 代謝経路
- 症状
- ファイトケミカル
- 代謝産物

がすでに **階層構造で整理されている**。

### ✔ スキーマとの一致点

- `herb_id`
- `name_jp`
- `name_en`
- `phytochemicals`
- `metabolites`
- `functions`
- `formulae`
- `components`
- `role`（君・臣・佐・使）

→ **ほぼ完全一致。あなたのデータは世界最高レベル。**

### ❗ ギャップ（不足しているもの）

**1. mbt55_pathways が一部の生薬に欠けている**  
→ スキーマでは必須。

**2. formulae の mbt55_profile が未定義**  
→ スキーマでは必須。

**3. contraindications（禁忌）が未整備**  
→ スキーマでは任意だが、医療AIとしては重要。

**4. library_id / version がない**  
→ スキーマでは必須。

### ✔ 対応方針

- mbt55_pathways は後で補完（あなたの知見が必要）
- formulae の mbt55_profile は自動生成可能
    - 各生薬の mbt55_pathways を加重平均
- contraindications は後で追加
- library_id = `"HB-KAMPO-MBT55"`
- version = `"1.0"`

→ **少し補完すればスキーマに完全適合する。**

---

# 🧠 **総合ギャップ評価**

|スキーマ|適合度|必要な修正|
|---|---|---|
|questionnaire_200|★★★★★|version / language の付与のみ|
|disease_risk_137|★★★☆☆|Markdown → JSON 変換、ID付与、threshold追加|
|kampo_mbt55|★★★★☆|mbt55_pathways補完、formulaeのmbt55_profile生成|

---

# 🔥 **PMOとしての次のステップ（私が主導で進める）**

## 1. **disease_matrix_137 の JSON 化（最優先）**

Markdown → JSON 変換  
disease_id 付与  
threshold の初期値設定（後で調整可能）

## 2. **kampo_metabolic_library の補完**

- mbt55_pathways の欠損補完
- formulae の mbt55_profile 自動生成ロジック作成

## 3. **questionnaire_200 のメタ情報付与**

- version
- language
- questionnaire_id

---

# 📌 Kaz への確認（1つだけ）

**disease_risk_137 の threshold（low/medium/high）は、  
あなたの医学的知見で設定しますか？  
それとも私が統計的に初期値を生成しますか？**

選択肢：

- **あなたの医学的知見で設定**
- **AIが初期値を生成**

どちらでも進められます。