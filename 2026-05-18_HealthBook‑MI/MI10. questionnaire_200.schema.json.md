お願いします。

---

いい、ここは一気に“芯”を固めにいきます。  
コア3スキーマを v1.0 ドラフトとしてまとめました。

---

### 1. `questionnaire_200.schema.json`（浜田式200問診）

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "HealthBook Questionnaire 200",
  "type": "object",
  "description": "浜田式200問診の構造定義",

  "properties": {
    "questionnaire_id": { "type": "string" },
    "language": { "type": "string", "enum": ["jp", "en"] },
    "version": { "type": "string" },

    "questions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "q_id": { "type": "string" },
          "category": { "type": "string" },
          "sub_category": { "type": "string" },
          "text": { "type": "string" },
          "type": {
            "type": "string",
            "enum": ["yes_no", "scale", "multi_choice", "free_text"]
          },
          "options": {
            "type": "array",
            "items": { "type": "string" }
          },
          "weight": { "type": "number" }
        },
        "required": ["q_id", "text", "type"]
      }
    }
  },

  "required": ["questionnaire_id", "language", "questions"]
}
```

---

### 2. `disease_risk_137.schema.json`（137疾病リスクマトリクス）

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "HealthBook Disease Risk 137",
  "type": "object",
  "description": "137疾病リスクと問診・検査項目の関連定義",

  "properties": {
    "matrix_id": { "type": "string" },
    "version": { "type": "string" },

    "diseases": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "disease_id": { "type": "string" },
          "name_jp": { "type": "string" },
          "name_en": { "type": "string" },
          "category": { "type": "string" },

          "risk_factors": {
            "type": "object",
            "description": "問診・血液・その他指標ごとの寄与",
            "properties": {
              "questionnaire": {
                "type": "object",
                "additionalProperties": { "type": "number" }
              },
              "blood": {
                "type": "object",
                "additionalProperties": { "type": "number" }
              },
              "phenotyping": {
                "type": "object",
                "additionalProperties": { "type": "number" }
              }
            }
          },

          "thresholds": {
            "type": "object",
            "properties": {
              "low": { "type": "number" },
              "medium": { "type": "number" },
              "high": { "type": "number" }
            }
          }
        },
        "required": ["disease_id", "name_jp"]
      }
    }
  },

  "required": ["matrix_id", "diseases"]
}
```

---

### 3. `kampo_mbt55.schema.json`（漢方 × MBT55 代謝マッピング）

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Kampo-MBT55 Metabolic Library",
  "type": "object",
  "description": "生薬・方剤・MBT55・代謝経路の統合定義",

  "properties": {
    "library_id": { "type": "string" },
    "version": { "type": "string" },

    "herbs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "herb_id": { "type": "string" },
          "name_jp": { "type": "string" },
          "name_en": { "type": "string" },

          "phytochemicals": {
            "type": "array",
            "items": { "type": "string" }
          },

          "mbt55_pathways": {
            "type": "object",
            "description": "MBT55各機能への寄与スコア",
            "additionalProperties": { "type": "number" }
          },

          "metabolites": {
            "type": "array",
            "items": { "type": "string" }
          },

          "functions": {
            "type": "array",
            "description": "中医学的・代謝学的機能",
            "items": { "type": "string" }
          },

          "contraindications": {
            "type": "array",
            "items": { "type": "string" }
          }
        },
        "required": ["herb_id", "name_jp"]
      }
    },

    "formulae": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "formula_id": { "type": "string" },
          "name_jp": { "type": "string" },
          "name_en": { "type": "string" },

          "components": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "herb_id": { "type": "string" },
                "role": {
                  "type": "string",
                  "enum": ["君", "臣", "佐", "使"]
                },
                "ratio": { "type": "number" }
              },
              "required": ["herb_id"]
            }
          },

          "mbt55_profile": {
            "type": "object",
            "description": "方剤全体としてのMBT55プロファイル",
            "additionalProperties": { "type": "number" }
          },

          "indications": {
            "type": "array",
            "description": "適応症・体質",
            "items": { "type": "string" }
          }
        },
        "required": ["formula_id", "name_jp"]
      }
    }
  },

  "required": ["library_id", "herbs"]
}
```

---

次の一手としては、この3つを前提に：

- MIスナップショットとの接続（どのフィールドにどう入るか）
- ダッシュボードの項目マッピング
- 実データ（あなたの既存JSON）とのギャップ確認

をやっていくフェーズです。

どこから詰めたいか、  
「スナップショットとの接続」か「ダッシュボード側」か、好みを教えて。